This folder is an example archive to keep the previous offering of the same course.
Notice that:
1. The `assets` folder is copied, but the relative paths does not need any fix.
2. The headers of all `*.md` files are properly updated with the `parent` page,
   and the `readme.md` is renamed to `index.md`.
3. The folder name starts with underscore `_` means "not compiling as pages" in the system.
   Rename it to activate.
