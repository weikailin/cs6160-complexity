This folder is an example archive to keep the previous offering of the same course.
Notice that the `assets` folder is copied, but the relative paths does not need any fix.
Also, the headers of all `*.md` files are properly updated with the `parent` page,
and the `readme.md` is renamed to `index.md`.
